{
  "name": "@studysphere/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Pure TypeScript domain logic shared by apps/mobile (and eventually apps/web): scheduling, revision, timer, summarizer, Sage, and date math. No React, no I/O.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
